import React from 'react';
import { FishingMapMarker } from '../../../../../../shared/types';

interface MarkerInfoProps {
  marker: FishingMapMarker;
}

const MarkerInfo: React.FC<any> = (props) => {
  return <></>;
};

export default MarkerInfo;
